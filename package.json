{
  "private": true,
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "start-app": "yarn workspace mobile start",
    "watch-packages": "yarn workspaces run build"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "name": "test-expo-modules-with-workspaces",
  "version": "1.0.0"
}
